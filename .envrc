# Go configuration - isolated per worktree
export GOCACHE="$(pwd)/.gocache"
export GOTMPDIR="$(pwd)/.gotmp"
export GOBIN="$(pwd)/bin"

# Shared Go module cache (safe for concurrent access)
export GOMODCACHE="$HOME/go/pkg/mod"