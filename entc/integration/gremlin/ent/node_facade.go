// Copyright 2019-present Facebook Inc. All rights reserved.
// This source code is licensed under the Apache 2.0 license found
// in the LICENSE file in the root directory of this source tree.

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"

	"entgo.io/ent/entc/integration/gremlin/ent/node"
	"entgo.io/ent/entc/integration/gremlin/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
)

// Type aliases — public consumer-facing names continue resolving here
// while the underlying types live in the per-entity sub-package.
type (
	NodeClient     = node.NodeClient
	NodeQuery      = node.NodeQuery
	NodeMutation   = node.NodeMutation
	NodeCreate     = node.NodeCreate
	NodeCreateBulk = node.NodeCreateBulk
	NodeUpdate     = node.NodeUpdate
	NodeUpdateOne  = node.NodeUpdateOne
	NodeDelete     = node.NodeDelete
	NodeDeleteOne  = node.NodeDeleteOne
	NodeGroupBy    = node.NodeGroupBy
	NodeSelect     = node.NodeSelect
)

// Constructor aliases — the sub-package's New<X> stays the source of truth.
var (
	NewNodeClient = node.NewNodeClient
)

// WithNodePrev eager-loads the "prev" edge on a NodeQuery. The
// optional arguments configure the sibling sub-query before storage.
func WithNodePrev(q *NodeQuery, opts ...func(*NodeQuery)) *NodeQuery {
	sub := NewNodeClient(q.Config).Query()
	for _, opt := range opts {
		opt(sub)
	}
	return q.StoreEager("prev", func(ctx context.Context, parents []*Node) error {
		return loadNodePrev(ctx, sub, parents)
	})
}

// QueryNodePrev returns a NodeQuery for the "prev" edge of a given Node.
func QueryNodePrev(c *NodeClient, _m *Node) *NodeQuery {
	query := NewNodeClient(c.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(node.Table, node.FieldID, id),
			sqlgraph.To(node.Table, node.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, node.PrevTable, node.PrevColumn),
		)
		fromV = sqlgraph.Neighbors(_m.Drv.Dialect(), step)

		return fromV, nil
	}
	return query
}

// loadNodePrev performs the eager-load for the "prev" edge. Body mirrors
// the pre-PR6 *NodeQuery.loadPrev method, hoisted to root
// so it can reference cross-package types directly.
func loadNodePrev(ctx context.Context, query *NodeQuery, nodes []*Node) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*Node)
	for i := range nodes {
		if nodes[i].GetNodeNext() == nil {
			continue
		}
		fk := *nodes[i].GetNodeNext()
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(node.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		parents, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "node_next" returned %v`, n.ID)
		}
		for i := range parents {
			parents[i].Edges.Prev = n
		}
	}
	return nil
}

// WithNodeNext eager-loads the "next" edge on a NodeQuery. The
// optional arguments configure the sibling sub-query before storage.
func WithNodeNext(q *NodeQuery, opts ...func(*NodeQuery)) *NodeQuery {
	sub := NewNodeClient(q.Config).Query()
	for _, opt := range opts {
		opt(sub)
	}
	return q.StoreEager("next", func(ctx context.Context, parents []*Node) error {
		return loadNodeNext(ctx, sub, parents)
	})
}

// QueryNodeNext returns a NodeQuery for the "next" edge of a given Node.
func QueryNodeNext(c *NodeClient, _m *Node) *NodeQuery {
	query := NewNodeClient(c.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(node.Table, node.FieldID, id),
			sqlgraph.To(node.Table, node.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, node.NextTable, node.NextColumn),
		)
		fromV = sqlgraph.Neighbors(_m.Drv.Dialect(), step)

		return fromV, nil
	}
	return query
}

// loadNodeNext performs the eager-load for the "next" edge. Body mirrors
// the pre-PR6 *NodeQuery.loadNext method, hoisted to root
// so it can reference cross-package types directly.
func loadNodeNext(ctx context.Context, query *NodeQuery, nodes []*Node) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Node)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	query.Where(predicate.Node(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(node.NextColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.GetNodeNext()
		if fk == nil {
			return fmt.Errorf(`foreign-key "node_next" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "node_next" returned %v for node %v`, *fk, n.ID)
		}
		node.Edges.Next = n
	}
	return nil
}
