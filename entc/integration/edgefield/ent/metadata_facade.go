// Copyright 2019-present Facebook Inc. All rights reserved.
// This source code is licensed under the Apache 2.0 license found
// in the LICENSE file in the root directory of this source tree.

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"

	"entgo.io/ent/entc/integration/edgefield/ent/metadata"
	"entgo.io/ent/entc/integration/edgefield/ent/predicate"
	"entgo.io/ent/entc/integration/edgefield/ent/user"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
)

// Type aliases — public consumer-facing names continue resolving here
// while the underlying types live in the per-entity sub-package.
type (
	MetadataClient     = metadata.MetadataClient
	MetadataQuery      = metadata.MetadataQuery
	MetadataMutation   = metadata.MetadataMutation
	MetadataCreate     = metadata.MetadataCreate
	MetadataCreateBulk = metadata.MetadataCreateBulk
	MetadataUpdate     = metadata.MetadataUpdate
	MetadataUpdateOne  = metadata.MetadataUpdateOne
	MetadataDelete     = metadata.MetadataDelete
	MetadataDeleteOne  = metadata.MetadataDeleteOne
	MetadataGroupBy    = metadata.MetadataGroupBy
	MetadataSelect     = metadata.MetadataSelect
)

// Constructor aliases — the sub-package's New<X> stays the source of truth.
var (
	NewMetadataClient = metadata.NewMetadataClient
)

// WithMetadataUser eager-loads the "user" edge on a MetadataQuery. The
// optional arguments configure the sibling sub-query before storage.
func WithMetadataUser(q *MetadataQuery, opts ...func(*UserQuery)) *MetadataQuery {
	sub := NewUserClient(q.Config).Query()
	for _, opt := range opts {
		opt(sub)
	}
	return q.StoreEager("user", func(ctx context.Context, parents []*Metadata) error {
		return loadMetadataUser(ctx, sub, parents)
	})
}

// QueryMetadataUser returns a UserQuery for the "user" edge of a given Metadata.
func QueryMetadataUser(c *MetadataClient, _m *Metadata) *UserQuery {
	query := NewUserClient(c.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(metadata.Table, metadata.FieldID, id),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, metadata.UserTable, metadata.UserColumn),
		)
		fromV = sqlgraph.Neighbors(_m.Drv.Dialect(), step)

		return fromV, nil
	}
	return query
}

// loadMetadataUser performs the eager-load for the "user" edge. Body mirrors
// the pre-PR6 *MetadataQuery.loadUser method, hoisted to root
// so it can reference cross-package types directly.
func loadMetadataUser(ctx context.Context, query *UserQuery, nodes []*Metadata) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Metadata)
	for i := range nodes {
		fk := nodes[i].ID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		parents, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "id" returned %v`, n.ID)
		}
		for i := range parents {
			parents[i].Edges.User = n
		}
	}
	return nil
}

// WithMetadataChildren eager-loads the "children" edge on a MetadataQuery. The
// optional arguments configure the sibling sub-query before storage.
func WithMetadataChildren(q *MetadataQuery, opts ...func(*MetadataQuery)) *MetadataQuery {
	sub := NewMetadataClient(q.Config).Query()
	for _, opt := range opts {
		opt(sub)
	}
	return q.StoreEager("children", func(ctx context.Context, parents []*Metadata) error {
		return loadMetadataChildren(ctx, sub, parents)
	})
}

// QueryMetadataChildren returns a MetadataQuery for the "children" edge of a given Metadata.
func QueryMetadataChildren(c *MetadataClient, _m *Metadata) *MetadataQuery {
	query := NewMetadataClient(c.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(metadata.Table, metadata.FieldID, id),
			sqlgraph.To(metadata.Table, metadata.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, metadata.ChildrenTable, metadata.ChildrenColumn),
		)
		fromV = sqlgraph.Neighbors(_m.Drv.Dialect(), step)

		return fromV, nil
	}
	return query
}

// loadMetadataChildren performs the eager-load for the "children" edge. Body mirrors
// the pre-PR6 *MetadataQuery.loadChildren method, hoisted to root
// so it can reference cross-package types directly.
func loadMetadataChildren(ctx context.Context, query *MetadataQuery, nodes []*Metadata) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Metadata)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		nodes[i].Edges.Children = []*Metadata{}
	}
	if len(query.Ctx.Fields) > 0 {
		query.Ctx.AppendFieldOnce(metadata.FieldParentID)
	}
	query.Where(predicate.Metadata(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(metadata.ChildrenColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ParentID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "parent_id" returned %v for node %v`, fk, n.ID)
		}
		node.Edges.Children = append(node.Edges.Children, n)
	}
	return nil
}

// WithMetadataParent eager-loads the "parent" edge on a MetadataQuery. The
// optional arguments configure the sibling sub-query before storage.
func WithMetadataParent(q *MetadataQuery, opts ...func(*MetadataQuery)) *MetadataQuery {
	sub := NewMetadataClient(q.Config).Query()
	for _, opt := range opts {
		opt(sub)
	}
	return q.StoreEager("parent", func(ctx context.Context, parents []*Metadata) error {
		return loadMetadataParent(ctx, sub, parents)
	})
}

// QueryMetadataParent returns a MetadataQuery for the "parent" edge of a given Metadata.
func QueryMetadataParent(c *MetadataClient, _m *Metadata) *MetadataQuery {
	query := NewMetadataClient(c.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(metadata.Table, metadata.FieldID, id),
			sqlgraph.To(metadata.Table, metadata.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, metadata.ParentTable, metadata.ParentColumn),
		)
		fromV = sqlgraph.Neighbors(_m.Drv.Dialect(), step)

		return fromV, nil
	}
	return query
}

// loadMetadataParent performs the eager-load for the "parent" edge. Body mirrors
// the pre-PR6 *MetadataQuery.loadParent method, hoisted to root
// so it can reference cross-package types directly.
func loadMetadataParent(ctx context.Context, query *MetadataQuery, nodes []*Metadata) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Metadata)
	for i := range nodes {
		fk := nodes[i].ParentID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(metadata.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		parents, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "parent_id" returned %v`, n.ID)
		}
		for i := range parents {
			parents[i].Edges.Parent = n
		}
	}
	return nil
}
