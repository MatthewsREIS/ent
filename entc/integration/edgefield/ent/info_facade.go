// Copyright 2019-present Facebook Inc. All rights reserved.
// This source code is licensed under the Apache 2.0 license found
// in the LICENSE file in the root directory of this source tree.

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"

	"entgo.io/ent/entc/integration/edgefield/ent/info"
	"entgo.io/ent/entc/integration/edgefield/ent/user"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
)

// Type aliases — public consumer-facing names continue resolving here
// while the underlying types live in the per-entity sub-package.
type (
	InfoClient     = info.InfoClient
	InfoQuery      = info.InfoQuery
	InfoMutation   = info.InfoMutation
	InfoCreate     = info.InfoCreate
	InfoCreateBulk = info.InfoCreateBulk
	InfoUpdate     = info.InfoUpdate
	InfoUpdateOne  = info.InfoUpdateOne
	InfoDelete     = info.InfoDelete
	InfoDeleteOne  = info.InfoDeleteOne
	InfoGroupBy    = info.InfoGroupBy
	InfoSelect     = info.InfoSelect
)

// Constructor aliases — the sub-package's New<X> stays the source of truth.
var (
	NewInfoClient = info.NewInfoClient
)

// WithInfoUser eager-loads the "user" edge on a InfoQuery. The
// optional arguments configure the sibling sub-query before storage.
func WithInfoUser(q *InfoQuery, opts ...func(*UserQuery)) *InfoQuery {
	sub := NewUserClient(q.Config).Query()
	for _, opt := range opts {
		opt(sub)
	}
	return q.StoreEager("user", func(ctx context.Context, parents []*Info) error {
		return loadInfoUser(ctx, sub, parents)
	})
}

// QueryInfoUser returns a UserQuery for the "user" edge of a given Info.
func QueryInfoUser(c *InfoClient, _m *Info) *UserQuery {
	query := NewUserClient(c.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(info.Table, info.FieldID, id),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, info.UserTable, info.UserColumn),
		)
		fromV = sqlgraph.Neighbors(_m.Drv.Dialect(), step)

		return fromV, nil
	}
	return query
}

// loadInfoUser performs the eager-load for the "user" edge. Body mirrors
// the pre-PR6 *InfoQuery.loadUser method, hoisted to root
// so it can reference cross-package types directly.
func loadInfoUser(ctx context.Context, query *UserQuery, nodes []*Info) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Info)
	for i := range nodes {
		fk := nodes[i].ID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		parents, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "id" returned %v`, n.ID)
		}
		for i := range parents {
			parents[i].Edges.User = n
		}
	}
	return nil
}
