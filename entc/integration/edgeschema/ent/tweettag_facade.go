// Copyright 2019-present Facebook Inc. All rights reserved.
// This source code is licensed under the Apache 2.0 license found
// in the LICENSE file in the root directory of this source tree.

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"

	"entgo.io/ent/entc/integration/edgeschema/ent/tag"
	"entgo.io/ent/entc/integration/edgeschema/ent/tweet"
	"entgo.io/ent/entc/integration/edgeschema/ent/tweettag"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
)

// Type aliases — public consumer-facing names continue resolving here
// while the underlying types live in the per-entity sub-package.
type (
	TweetTagClient     = tweettag.TweetTagClient
	TweetTagQuery      = tweettag.TweetTagQuery
	TweetTagMutation   = tweettag.TweetTagMutation
	TweetTagCreate     = tweettag.TweetTagCreate
	TweetTagCreateBulk = tweettag.TweetTagCreateBulk
	TweetTagUpdate     = tweettag.TweetTagUpdate
	TweetTagUpdateOne  = tweettag.TweetTagUpdateOne
	TweetTagDelete     = tweettag.TweetTagDelete
	TweetTagDeleteOne  = tweettag.TweetTagDeleteOne
	TweetTagGroupBy    = tweettag.TweetTagGroupBy
	TweetTagSelect     = tweettag.TweetTagSelect
	TweetTagFilter     = tweettag.TweetTagFilter
)

// Constructor aliases — the sub-package's New<X> stays the source of truth.
var (
	NewTweetTagClient            = tweettag.NewTweetTagClient
	NewTweetTagFilterForMutation = tweettag.NewTweetTagFilterForMutation
)

// WithTweetTagTag eager-loads the "tag" edge on a TweetTagQuery. The
// optional arguments configure the sibling sub-query before storage.
func WithTweetTagTag(q *TweetTagQuery, opts ...func(*TagQuery)) *TweetTagQuery {
	sub := NewTagClient(q.Config).Query()
	for _, opt := range opts {
		opt(sub)
	}
	return q.StoreEager("tag", func(ctx context.Context, parents []*TweetTag) error {
		return loadTweetTagTag(ctx, sub, parents)
	})
}

// QueryTweetTagTag returns a TagQuery for the "tag" edge of a given TweetTag.
func QueryTweetTagTag(c *TweetTagClient, _m *TweetTag) *TagQuery {
	query := NewTagClient(c.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(tweettag.Table, tweettag.FieldID, id),
			sqlgraph.To(tag.Table, tag.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, tweettag.TagTable, tweettag.TagColumn),
		)
		fromV = sqlgraph.Neighbors(_m.Drv.Dialect(), step)

		return fromV, nil
	}
	return query
}

// loadTweetTagTag performs the eager-load for the "tag" edge. Body mirrors
// the pre-PR6 *TweetTagQuery.loadTag method, hoisted to root
// so it can reference cross-package types directly.
func loadTweetTagTag(ctx context.Context, query *TagQuery, nodes []*TweetTag) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*TweetTag)
	for i := range nodes {
		fk := nodes[i].TagID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(tag.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		parents, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "tag_id" returned %v`, n.ID)
		}
		for i := range parents {
			parents[i].Edges.Tag = n
		}
	}
	return nil
}

// WithTweetTagTweet eager-loads the "tweet" edge on a TweetTagQuery. The
// optional arguments configure the sibling sub-query before storage.
func WithTweetTagTweet(q *TweetTagQuery, opts ...func(*TweetQuery)) *TweetTagQuery {
	sub := NewTweetClient(q.Config).Query()
	for _, opt := range opts {
		opt(sub)
	}
	return q.StoreEager("tweet", func(ctx context.Context, parents []*TweetTag) error {
		return loadTweetTagTweet(ctx, sub, parents)
	})
}

// QueryTweetTagTweet returns a TweetQuery for the "tweet" edge of a given TweetTag.
func QueryTweetTagTweet(c *TweetTagClient, _m *TweetTag) *TweetQuery {
	query := NewTweetClient(c.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(tweettag.Table, tweettag.FieldID, id),
			sqlgraph.To(tweet.Table, tweet.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, tweettag.TweetTable, tweettag.TweetColumn),
		)
		fromV = sqlgraph.Neighbors(_m.Drv.Dialect(), step)

		return fromV, nil
	}
	return query
}

// loadTweetTagTweet performs the eager-load for the "tweet" edge. Body mirrors
// the pre-PR6 *TweetTagQuery.loadTweet method, hoisted to root
// so it can reference cross-package types directly.
func loadTweetTagTweet(ctx context.Context, query *TweetQuery, nodes []*TweetTag) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*TweetTag)
	for i := range nodes {
		fk := nodes[i].TweetID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(tweet.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		parents, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "tweet_id" returned %v`, n.ID)
		}
		for i := range parents {
			parents[i].Edges.Tweet = n
		}
	}
	return nil
}
