// Copyright 2019-present Facebook Inc. All rights reserved.
// This source code is licensed under the Apache 2.0 license found
// in the LICENSE file in the root directory of this source tree.

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"

	"entgo.io/ent/entc/integration/edgeschema/ent/group"
	"entgo.io/ent/entc/integration/edgeschema/ent/grouptag"
	"entgo.io/ent/entc/integration/edgeschema/ent/tag"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
)

// Type aliases — public consumer-facing names continue resolving here
// while the underlying types live in the per-entity sub-package.
type (
	GroupTagClient     = grouptag.GroupTagClient
	GroupTagQuery      = grouptag.GroupTagQuery
	GroupTagMutation   = grouptag.GroupTagMutation
	GroupTagCreate     = grouptag.GroupTagCreate
	GroupTagCreateBulk = grouptag.GroupTagCreateBulk
	GroupTagUpdate     = grouptag.GroupTagUpdate
	GroupTagUpdateOne  = grouptag.GroupTagUpdateOne
	GroupTagDelete     = grouptag.GroupTagDelete
	GroupTagDeleteOne  = grouptag.GroupTagDeleteOne
	GroupTagGroupBy    = grouptag.GroupTagGroupBy
	GroupTagSelect     = grouptag.GroupTagSelect
	GroupTagFilter     = grouptag.GroupTagFilter
)

// Constructor aliases — the sub-package's New<X> stays the source of truth.
var (
	NewGroupTagClient            = grouptag.NewGroupTagClient
	NewGroupTagFilterForMutation = grouptag.NewGroupTagFilterForMutation
)

// WithGroupTagTag eager-loads the "tag" edge on a GroupTagQuery. The
// optional arguments configure the sibling sub-query before storage.
func WithGroupTagTag(q *GroupTagQuery, opts ...func(*TagQuery)) *GroupTagQuery {
	sub := NewTagClient(q.Config).Query()
	for _, opt := range opts {
		opt(sub)
	}
	return q.StoreEager("tag", func(ctx context.Context, parents []*GroupTag) error {
		return loadGroupTagTag(ctx, sub, parents)
	})
}

// QueryGroupTagTag returns a TagQuery for the "tag" edge of a given GroupTag.
func QueryGroupTagTag(c *GroupTagClient, _m *GroupTag) *TagQuery {
	query := NewTagClient(c.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(grouptag.Table, grouptag.FieldID, id),
			sqlgraph.To(tag.Table, tag.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, grouptag.TagTable, grouptag.TagColumn),
		)
		fromV = sqlgraph.Neighbors(_m.Drv.Dialect(), step)

		return fromV, nil
	}
	return query
}

// loadGroupTagTag performs the eager-load for the "tag" edge. Body mirrors
// the pre-PR6 *GroupTagQuery.loadTag method, hoisted to root
// so it can reference cross-package types directly.
func loadGroupTagTag(ctx context.Context, query *TagQuery, nodes []*GroupTag) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*GroupTag)
	for i := range nodes {
		fk := nodes[i].TagID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(tag.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		parents, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "tag_id" returned %v`, n.ID)
		}
		for i := range parents {
			parents[i].Edges.Tag = n
		}
	}
	return nil
}

// WithGroupTagGroup eager-loads the "group" edge on a GroupTagQuery. The
// optional arguments configure the sibling sub-query before storage.
func WithGroupTagGroup(q *GroupTagQuery, opts ...func(*GroupQuery)) *GroupTagQuery {
	sub := NewGroupClient(q.Config).Query()
	for _, opt := range opts {
		opt(sub)
	}
	return q.StoreEager("group", func(ctx context.Context, parents []*GroupTag) error {
		return loadGroupTagGroup(ctx, sub, parents)
	})
}

// QueryGroupTagGroup returns a GroupQuery for the "group" edge of a given GroupTag.
func QueryGroupTagGroup(c *GroupTagClient, _m *GroupTag) *GroupQuery {
	query := NewGroupClient(c.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(grouptag.Table, grouptag.FieldID, id),
			sqlgraph.To(group.Table, group.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, grouptag.GroupTable, grouptag.GroupColumn),
		)
		fromV = sqlgraph.Neighbors(_m.Drv.Dialect(), step)

		return fromV, nil
	}
	return query
}

// loadGroupTagGroup performs the eager-load for the "group" edge. Body mirrors
// the pre-PR6 *GroupTagQuery.loadGroup method, hoisted to root
// so it can reference cross-package types directly.
func loadGroupTagGroup(ctx context.Context, query *GroupQuery, nodes []*GroupTag) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*GroupTag)
	for i := range nodes {
		fk := nodes[i].GroupID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(group.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		parents, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "group_id" returned %v`, n.ID)
		}
		for i := range parents {
			parents[i].Edges.Group = n
		}
	}
	return nil
}
