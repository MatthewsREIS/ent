// Copyright 2019-present Facebook Inc. All rights reserved.
// This source code is licensed under the Apache 2.0 license found
// in the LICENSE file in the root directory of this source tree.

// Code generated by ent, DO NOT EDIT.

package tweetlike

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/entc/integration/edgeschema/ent/predicate"
	"entgo.io/ent/runtime/entbuilder"
)

// TweetLikeQuery is the builder for querying TweetLike entities.
type TweetLikeQuery struct {
	Config
	entbuilder.QueryState[predicate.TweetLike]
	order        []OrderOption
	eagerLoaders map[string]func(context.Context, []*TweetLike) error
}

// StoreEager registers an eager-load callback for the given edge name.
// Exposed for the root facade's With<Entity><Edge> free functions.
func (_q *TweetLikeQuery) StoreEager(name string, loader func(context.Context, []*TweetLike) error) *TweetLikeQuery {
	if _q.eagerLoaders == nil {
		_q.eagerLoaders = make(map[string]func(context.Context, []*TweetLike) error)
	}
	_q.eagerLoaders[name] = loader
	return _q
}

// EagerLoaders returns the registered eager-load callbacks (read-only).
// Used by the sub-package's All() (Task 13) and by root facade utilities.
func (_q *TweetLikeQuery) EagerLoaders() map[string]func(context.Context, []*TweetLike) error {
	return _q.eagerLoaders
}

// Where adds a new predicate for the TweetLikeQuery builder.
func (_q *TweetLikeQuery) Where(ps ...predicate.TweetLike) *TweetLikeQuery {
	_q.AddPredicates(ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *TweetLikeQuery) Limit(limit int) *TweetLikeQuery {
	_q.SetLimit(limit)
	return _q
}

// Offset to start from.
func (_q *TweetLikeQuery) Offset(offset int) *TweetLikeQuery {
	_q.SetOffset(offset)
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *TweetLikeQuery) Unique(unique bool) *TweetLikeQuery {
	_q.SetUnique(unique)
	return _q
}

// Order specifies how the records should be ordered.
func (_q *TweetLikeQuery) Order(o ...OrderOption) *TweetLikeQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// First returns the first TweetLike entity from the query.
// Returns a *NotFoundError when no TweetLike was found.
func (_q *TweetLikeQuery) First(ctx context.Context) (*TweetLike, error) {
	_q.Limit(1)
	return entbuilder.RunFirst[*TweetLike, []*TweetLike](ctx, _q, _q.Ctx, ent.OpQueryFirst, Label, _q.QueryState.Inters, _q.prepareQuery, func(ctx context.Context) ([]*TweetLike, error) { return _q.sqlAll(ctx) }, func(label string) error { return &NotFoundError{Label: label} })
}

// FirstX is like First, but panics if an error occurs.
func (_q *TweetLikeQuery) FirstX(ctx context.Context) *TweetLike {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// Only returns a single TweetLike entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TweetLike entity is found.
// Returns a *NotFoundError when no TweetLike entities are found.
func (_q *TweetLikeQuery) Only(ctx context.Context) (*TweetLike, error) {
	_q.Limit(2)
	return entbuilder.RunOnly[*TweetLike, []*TweetLike](ctx, _q, _q.Ctx, ent.OpQueryOnly, Label, _q.QueryState.Inters, _q.prepareQuery, func(ctx context.Context) ([]*TweetLike, error) { return _q.sqlAll(ctx) }, func(label string) error { return &NotFoundError{Label: label} }, func(label string) error { return &NotSingularError{Label: label} })
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *TweetLikeQuery) OnlyX(ctx context.Context) *TweetLike {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// All executes the query and returns a list of TweetLikes.
func (_q *TweetLikeQuery) All(ctx context.Context) ([]*TweetLike, error) {
	return entbuilder.RunAll[[]*TweetLike](ctx, _q, _q.Ctx, ent.OpQueryAll, _q.QueryState.Inters, _q.prepareQuery, func(ctx context.Context) ([]*TweetLike, error) { return _q.sqlAll(ctx) })
}

// AllX is like All, but panics if an error occurs.
func (_q *TweetLikeQuery) AllX(ctx context.Context) []*TweetLike {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// Count returns the count of the given query.
func (_q *TweetLikeQuery) Count(ctx context.Context) (int, error) {
	return entbuilder.RunCount(ctx, _q, _q.Ctx, ent.OpQueryCount, _q.QueryState.Inters, _q.prepareQuery, _q.sqlCount)
}

// CountX is like Count, but panics if an error occurs.
func (_q *TweetLikeQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *TweetLikeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.Ctx, ent.OpQueryExist)
	switch _, err := _q.First(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *TweetLikeQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TweetLikeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *TweetLikeQuery) Clone() *TweetLikeQuery {
	if _q == nil {
		return nil
	}
	return &TweetLikeQuery{
		Config:       _q.Config,
		QueryState:   *_q.QueryState.Clone(),
		order:        append([]OrderOption{}, _q.order...),
		eagerLoaders: cloneEagerLoadersTweetLike(_q.eagerLoaders),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		LikedAt time.Time `json:"liked_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TweetLike.Query().
//		GroupBy(FieldLikedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *TweetLikeQuery) GroupBy(field string, fields ...string) *TweetLikeGroupBy {
	_q.Ctx.Fields = append([]string{field}, fields...)
	grbuild := &TweetLikeGroupBy{build: _q}
	grbuild.Flds = &_q.Ctx.Fields
	grbuild.Label = Label
	grbuild.ScanFn = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		LikedAt time.Time `json:"liked_at,omitempty"`
//	}
//
//	client.TweetLike.Query().
//		Select(FieldLikedAt).
//		Scan(ctx, &v)
func (_q *TweetLikeQuery) Select(fields ...string) *TweetLikeSelect {
	_q.Ctx.Fields = append(_q.Ctx.Fields, fields...)
	sbuild := &TweetLikeSelect{TweetLikeQuery: _q}
	sbuild.Label = Label
	sbuild.Flds, sbuild.ScanFn = &_q.Ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TweetLikeSelect configured with the given aggregations.
func (_q *TweetLikeQuery) Aggregate(fns ...AggregateFunc) *TweetLikeSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *TweetLikeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.QueryState.Inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.Ctx.Fields {
		if !ValidColumn(f) {
			return &ValidationError{Name: f, Err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.Path != nil {
		prev, err := _q.Path(ctx)
		if err != nil {
			return err
		}
		_q.Sql = prev
	}
	if Policy == nil {
		return errors.New("ent: uninitialized Policy (forgotten import ent/runtime?)")
	}
	if err := Policy.EvalQuery(ctx, _q); err != nil {
		return err
	}
	return nil
}

func (_q *TweetLikeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TweetLike, error) {
	nodes, err := _q.Fetch(ctx, hooks...)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	// Dispatch any registered eager loaders. Callbacks were captured by
	// the root facade's With<Entity><Edge> free functions and receive the
	// parent slice for in-place edge assignment. Dispatch lives in sqlAll
	// (not All) so Only/First/IDs/etc. — which all call sqlAll — also
	// fire registered loaders. Fetch is the no-dispatch alternative used
	// by load helpers themselves to avoid recursion.
	for _, loader := range _q.eagerLoaders {
		if err := loader(ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// Fetch executes the SQL parent query only — no eager loading.
// Exposed for the root facade's edge-loading helpers (Task 17), which
// call Fetch on a sub-query to materialize neighbors during eager dispatch.
// Called internally by sqlAll, which wraps Fetch with the eager-loader
// dispatch loop.
func (_q *TweetLikeQuery) Fetch(ctx context.Context, hooks ...queryHook) ([]*TweetLike, error) {
	var (
		nodes       = []*TweetLike{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.eagerLoaders["tweet"] != nil,
			_q.eagerLoaders["user"] != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TweetLike).ScanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TweetLike{Config: _q.Config}
		nodes = append(nodes, node)
		node.Edges.SetLoadedTypes(loadedTypes)
		return node.AssignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.Drv, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

// PrepareQuery is the exported counterpart of prepareQuery — used by
// root facade eager-load helpers that build join predicates and need
// to attach interceptors/hooks before executing the sub-query.
func (_q *TweetLikeQuery) PrepareQuery(ctx context.Context) error {
	return _q.prepareQuery(ctx)
}

// SQLQuery is the exported counterpart of sqlQuery — used by root
// facade chained-edge helpers (e.g., QueryXEdgeFromQuery) that need
// this query's SQL selector to construct a SetNeighbors sub-step.
func (_q *TweetLikeQuery) SQLQuery(ctx context.Context) *sql.Selector {
	return _q.sqlQuery(ctx)
}

// IncludeForeignKeys is a no-op for types with no unexported foreign-key
// columns — every column is already in the SELECT. Emitted so root facade
// load helpers can call it unconditionally without compile-time branching
// on the loaded sibling's FK shape.
func (_q *TweetLikeQuery) IncludeForeignKeys(bool) *TweetLikeQuery {
	return _q
}

func (_q *TweetLikeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Unique = false
	_spec.Node.Columns = nil
	return sqlgraph.CountNodes(ctx, _q.Drv, _spec)
}

func (_q *TweetLikeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(Table, Columns, nil)
	_spec.From = _q.Sql
	if unique := _q.Ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.Path != nil {
		_spec.Unique = true
	}
	if fields := _q.Ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		for i := range fields {
			_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
		}
		if _, ok := _q.eagerLoaders["tweet"]; ok {
			_spec.Node.AddColumnOnce(FieldTweetID)
		}
		if _, ok := _q.eagerLoaders["user"]; ok {
			_spec.Node.AddColumnOnce(FieldUserID)
		}
	}
	if ps := _q.Predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.Ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.Ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *TweetLikeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.Drv.Dialect())
	t1 := builder.Table(Table)
	columns := _q.Ctx.Fields
	if len(columns) == 0 {
		columns = Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.Sql != nil {
		selector = _q.Sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.Ctx.Unique != nil && *_q.Ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.Predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.Ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.Ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TweetLikeGroupBy is the group-by builder for TweetLike entities.
type TweetLikeGroupBy struct {
	selector
	build *TweetLikeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *TweetLikeGroupBy) Aggregate(fns ...AggregateFunc) *TweetLikeGroupBy {
	_g.Fns = append(_g.Fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *TweetLikeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.Ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TweetLikeQuery, *TweetLikeGroupBy](ctx, _g.build, _g, _g.build.QueryState.Inters, v)
}

func (_g *TweetLikeGroupBy) sqlScan(ctx context.Context, root *TweetLikeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.Fns))
	for _, fn := range _g.Fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.Flds)+len(_g.Fns))
		for _, f := range *_g.Flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.Flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.Drv.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TweetLikeSelect is the builder for selecting fields of TweetLike entities.
type TweetLikeSelect struct {
	*TweetLikeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *TweetLikeSelect) Aggregate(fns ...AggregateFunc) *TweetLikeSelect {
	_s.Fns = append(_s.Fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *TweetLikeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.Ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TweetLikeQuery, *TweetLikeSelect](ctx, _s.TweetLikeQuery, _s, _s.QueryState.Inters, v)
}

func (_s *TweetLikeSelect) sqlScan(ctx context.Context, root *TweetLikeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.Fns))
	for _, fn := range _s.Fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.Flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.Drv.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// cloneEagerLoadersTweetLike returns a shallow copy of the eager-loaders map.
// The closures themselves are shared between original and clone.
func cloneEagerLoadersTweetLike(in map[string]func(context.Context, []*TweetLike) error) map[string]func(context.Context, []*TweetLike) error {
	if in == nil {
		return nil
	}
	out := make(map[string]func(context.Context, []*TweetLike) error, len(in))
	for k, v := range in {
		out[k] = v
	}
	return out
}
