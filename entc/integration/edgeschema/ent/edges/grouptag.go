// Copyright 2019-present Facebook Inc. All rights reserved.
// This source code is licensed under the Apache 2.0 license found
// in the LICENSE file in the root directory of this source tree.

// Code generated by ent, DO NOT EDIT.

package edges

import (
	"context"
	"fmt"

	"entgo.io/ent/entc/integration/edgeschema/ent/group"
	"entgo.io/ent/entc/integration/edgeschema/ent/grouptag"
	"entgo.io/ent/entc/integration/edgeschema/ent/tag"
)

// LoadGroupTagTag performs the eager-load for the "tag" edge. Body mirrors
// the pre-PR6 *GroupTagQuery.loadTag method, hoisted to root
// so it can reference cross-package types directly.
func LoadGroupTagTag(ctx context.Context, query *tag.TagQuery, nodes []*grouptag.GroupTag) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*grouptag.GroupTag)
	for i := range nodes {
		fk := nodes[i].TagID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(tag.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		parents, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "tag_id" returned %v`, n.ID)
		}
		for i := range parents {
			parents[i].Edges.Tag = n
		}
	}
	return nil
}

// LoadGroupTagGroup performs the eager-load for the "group" edge. Body mirrors
// the pre-PR6 *GroupTagQuery.loadGroup method, hoisted to root
// so it can reference cross-package types directly.
func LoadGroupTagGroup(ctx context.Context, query *group.GroupQuery, nodes []*grouptag.GroupTag) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*grouptag.GroupTag)
	for i := range nodes {
		fk := nodes[i].GroupID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(group.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		parents, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "group_id" returned %v`, n.ID)
		}
		for i := range parents {
			parents[i].Edges.Group = n
		}
	}
	return nil
}
