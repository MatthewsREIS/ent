// Copyright 2019-present Facebook Inc. All rights reserved.
// This source code is licensed under the Apache 2.0 license found
// in the LICENSE file in the root directory of this source tree.

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"

	"entgo.io/ent/entc/integration/edgeschema/ent/attachedfile"
	"entgo.io/ent/entc/integration/edgeschema/ent/file"
	"entgo.io/ent/entc/integration/edgeschema/ent/process"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
)

// Type aliases — public consumer-facing names continue resolving here
// while the underlying types live in the per-entity sub-package.
type (
	AttachedFileClient     = attachedfile.AttachedFileClient
	AttachedFileQuery      = attachedfile.AttachedFileQuery
	AttachedFileMutation   = attachedfile.AttachedFileMutation
	AttachedFileCreate     = attachedfile.AttachedFileCreate
	AttachedFileCreateBulk = attachedfile.AttachedFileCreateBulk
	AttachedFileUpdate     = attachedfile.AttachedFileUpdate
	AttachedFileUpdateOne  = attachedfile.AttachedFileUpdateOne
	AttachedFileDelete     = attachedfile.AttachedFileDelete
	AttachedFileDeleteOne  = attachedfile.AttachedFileDeleteOne
	AttachedFileGroupBy    = attachedfile.AttachedFileGroupBy
	AttachedFileSelect     = attachedfile.AttachedFileSelect
	AttachedFileFilter     = attachedfile.AttachedFileFilter
)

// Constructor aliases — the sub-package's New<X> stays the source of truth.
var (
	NewAttachedFileClient            = attachedfile.NewAttachedFileClient
	NewAttachedFileFilterForMutation = attachedfile.NewAttachedFileFilterForMutation
)

// WithAttachedFileFi eager-loads the "fi" edge on a AttachedFileQuery. The
// optional arguments configure the sibling sub-query before storage.
func WithAttachedFileFi(q *AttachedFileQuery, opts ...func(*FileQuery)) *AttachedFileQuery {
	sub := NewFileClient(q.Config).Query()
	for _, opt := range opts {
		opt(sub)
	}
	return q.StoreEager("fi", func(ctx context.Context, parents []*AttachedFile) error {
		return loadAttachedFileFi(ctx, sub, parents)
	})
}

// QueryAttachedFileFi returns a FileQuery for the "fi" edge of a given AttachedFile.
func QueryAttachedFileFi(c *AttachedFileClient, _m *AttachedFile) *FileQuery {
	query := NewFileClient(c.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(attachedfile.Table, attachedfile.FieldID, id),
			sqlgraph.To(file.Table, file.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, attachedfile.FiTable, attachedfile.FiColumn),
		)
		fromV = sqlgraph.Neighbors(_m.Drv.Dialect(), step)

		return fromV, nil
	}
	return query
}

// loadAttachedFileFi performs the eager-load for the "fi" edge. Body mirrors
// the pre-PR6 *AttachedFileQuery.loadFi method, hoisted to root
// so it can reference cross-package types directly.
func loadAttachedFileFi(ctx context.Context, query *FileQuery, nodes []*AttachedFile) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*AttachedFile)
	for i := range nodes {
		fk := nodes[i].FID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(file.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		parents, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "f_id" returned %v`, n.ID)
		}
		for i := range parents {
			parents[i].Edges.Fi = n
		}
	}
	return nil
}

// WithAttachedFileProc eager-loads the "proc" edge on a AttachedFileQuery. The
// optional arguments configure the sibling sub-query before storage.
func WithAttachedFileProc(q *AttachedFileQuery, opts ...func(*ProcessQuery)) *AttachedFileQuery {
	sub := NewProcessClient(q.Config).Query()
	for _, opt := range opts {
		opt(sub)
	}
	return q.StoreEager("proc", func(ctx context.Context, parents []*AttachedFile) error {
		return loadAttachedFileProc(ctx, sub, parents)
	})
}

// QueryAttachedFileProc returns a ProcessQuery for the "proc" edge of a given AttachedFile.
func QueryAttachedFileProc(c *AttachedFileClient, _m *AttachedFile) *ProcessQuery {
	query := NewProcessClient(c.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(attachedfile.Table, attachedfile.FieldID, id),
			sqlgraph.To(process.Table, process.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, attachedfile.ProcTable, attachedfile.ProcColumn),
		)
		fromV = sqlgraph.Neighbors(_m.Drv.Dialect(), step)

		return fromV, nil
	}
	return query
}

// loadAttachedFileProc performs the eager-load for the "proc" edge. Body mirrors
// the pre-PR6 *AttachedFileQuery.loadProc method, hoisted to root
// so it can reference cross-package types directly.
func loadAttachedFileProc(ctx context.Context, query *ProcessQuery, nodes []*AttachedFile) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*AttachedFile)
	for i := range nodes {
		fk := nodes[i].ProcID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(process.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		parents, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "proc_id" returned %v`, n.ID)
		}
		for i := range parents {
			parents[i].Edges.Proc = n
		}
	}
	return nil
}
