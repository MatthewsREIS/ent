// Copyright 2019-present Facebook Inc. All rights reserved.
// This source code is licensed under the Apache 2.0 license found
// in the LICENSE file in the root directory of this source tree.

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"

	"entgo.io/ent/entc/integration/edgeschema/ent/relationship"
	"entgo.io/ent/entc/integration/edgeschema/ent/relationshipinfo"
	"entgo.io/ent/entc/integration/edgeschema/ent/user"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
)

// Type aliases — public consumer-facing names continue resolving here
// while the underlying types live in the per-entity sub-package.
type (
	RelationshipClient     = relationship.RelationshipClient
	RelationshipQuery      = relationship.RelationshipQuery
	RelationshipMutation   = relationship.RelationshipMutation
	RelationshipCreate     = relationship.RelationshipCreate
	RelationshipCreateBulk = relationship.RelationshipCreateBulk
	RelationshipUpdate     = relationship.RelationshipUpdate
	RelationshipUpdateOne  = relationship.RelationshipUpdateOne
	RelationshipDelete     = relationship.RelationshipDelete
	RelationshipDeleteOne  = relationship.RelationshipDeleteOne
	RelationshipGroupBy    = relationship.RelationshipGroupBy
	RelationshipSelect     = relationship.RelationshipSelect
	RelationshipFilter     = relationship.RelationshipFilter
)

// Constructor aliases — the sub-package's New<X> stays the source of truth.
var (
	NewRelationshipClient            = relationship.NewRelationshipClient
	NewRelationshipFilterForMutation = relationship.NewRelationshipFilterForMutation
)

// WithRelationshipUser eager-loads the "user" edge on a RelationshipQuery. The
// optional arguments configure the sibling sub-query before storage.
func WithRelationshipUser(q *RelationshipQuery, opts ...func(*UserQuery)) *RelationshipQuery {
	sub := NewUserClient(q.Config).Query()
	for _, opt := range opts {
		opt(sub)
	}
	return q.StoreEager("user", func(ctx context.Context, parents []*Relationship) error {
		return loadRelationshipUser(ctx, sub, parents)
	})
}

// QueryRelationshipUser returns a UserQuery for the "user" edge of a given Relationship.
func QueryRelationshipUser(c *RelationshipClient, _m *Relationship) *UserQuery {
	query := NewUserClient(c.Config).Query()
	return query
}

// QueryRelationshipUserFromQuery returns a UserQuery that traverses the "user" edge
// of every Relationship matched by q (chained-query form). Mirrors the pre-PR6
// (*RelationshipQuery).QueryUser method, hoisted to root so it
// can reference the cross-package UserQuery type.
func QueryRelationshipUserFromQuery(q *RelationshipQuery) *UserQuery {
	query := NewUserClient(q.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, err error) {
		if err := q.PrepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := q.SQLQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(relationship.Table, relationship.UserColumn, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, relationship.UserTable, relationship.UserColumn),
		)
		fromV = sqlgraph.SetNeighbors(q.Drv.Dialect(), step)
		return fromV, nil
	}
	return query
}

// loadRelationshipUser performs the eager-load for the "user" edge. Body mirrors
// the pre-PR6 *RelationshipQuery.loadUser method, hoisted to root
// so it can reference cross-package types directly.
func loadRelationshipUser(ctx context.Context, query *UserQuery, nodes []*Relationship) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Relationship)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		parents, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range parents {
			parents[i].Edges.User = n
		}
	}
	return nil
}

// WithRelationshipRelative eager-loads the "relative" edge on a RelationshipQuery. The
// optional arguments configure the sibling sub-query before storage.
func WithRelationshipRelative(q *RelationshipQuery, opts ...func(*UserQuery)) *RelationshipQuery {
	sub := NewUserClient(q.Config).Query()
	for _, opt := range opts {
		opt(sub)
	}
	return q.StoreEager("relative", func(ctx context.Context, parents []*Relationship) error {
		return loadRelationshipRelative(ctx, sub, parents)
	})
}

// QueryRelationshipRelative returns a UserQuery for the "relative" edge of a given Relationship.
func QueryRelationshipRelative(c *RelationshipClient, _m *Relationship) *UserQuery {
	query := NewUserClient(c.Config).Query()
	return query
}

// QueryRelationshipRelativeFromQuery returns a UserQuery that traverses the "relative" edge
// of every Relationship matched by q (chained-query form). Mirrors the pre-PR6
// (*RelationshipQuery).QueryRelative method, hoisted to root so it
// can reference the cross-package UserQuery type.
func QueryRelationshipRelativeFromQuery(q *RelationshipQuery) *UserQuery {
	query := NewUserClient(q.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, err error) {
		if err := q.PrepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := q.SQLQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(relationship.Table, relationship.RelativeColumn, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, relationship.RelativeTable, relationship.RelativeColumn),
		)
		fromV = sqlgraph.SetNeighbors(q.Drv.Dialect(), step)
		return fromV, nil
	}
	return query
}

// loadRelationshipRelative performs the eager-load for the "relative" edge. Body mirrors
// the pre-PR6 *RelationshipQuery.loadRelative method, hoisted to root
// so it can reference cross-package types directly.
func loadRelationshipRelative(ctx context.Context, query *UserQuery, nodes []*Relationship) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Relationship)
	for i := range nodes {
		fk := nodes[i].RelativeID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		parents, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "relative_id" returned %v`, n.ID)
		}
		for i := range parents {
			parents[i].Edges.Relative = n
		}
	}
	return nil
}

// WithRelationshipInfo eager-loads the "info" edge on a RelationshipQuery. The
// optional arguments configure the sibling sub-query before storage.
func WithRelationshipInfo(q *RelationshipQuery, opts ...func(*RelationshipInfoQuery)) *RelationshipQuery {
	sub := NewRelationshipInfoClient(q.Config).Query()
	for _, opt := range opts {
		opt(sub)
	}
	return q.StoreEager("info", func(ctx context.Context, parents []*Relationship) error {
		return loadRelationshipInfo(ctx, sub, parents)
	})
}

// QueryRelationshipInfo returns a RelationshipInfoQuery for the "info" edge of a given Relationship.
func QueryRelationshipInfo(c *RelationshipClient, _m *Relationship) *RelationshipInfoQuery {
	query := NewRelationshipInfoClient(c.Config).Query()
	return query
}

// QueryRelationshipInfoFromQuery returns a RelationshipInfoQuery that traverses the "info" edge
// of every Relationship matched by q (chained-query form). Mirrors the pre-PR6
// (*RelationshipQuery).QueryInfo method, hoisted to root so it
// can reference the cross-package RelationshipInfoQuery type.
func QueryRelationshipInfoFromQuery(q *RelationshipQuery) *RelationshipInfoQuery {
	query := NewRelationshipInfoClient(q.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, err error) {
		if err := q.PrepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := q.SQLQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(relationship.Table, relationship.InfoColumn, selector),
			sqlgraph.To(relationshipinfo.Table, relationshipinfo.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, relationship.InfoTable, relationship.InfoColumn),
		)
		fromV = sqlgraph.SetNeighbors(q.Drv.Dialect(), step)
		return fromV, nil
	}
	return query
}

// loadRelationshipInfo performs the eager-load for the "info" edge. Body mirrors
// the pre-PR6 *RelationshipQuery.loadInfo method, hoisted to root
// so it can reference cross-package types directly.
func loadRelationshipInfo(ctx context.Context, query *RelationshipInfoQuery, nodes []*Relationship) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Relationship)
	for i := range nodes {
		fk := nodes[i].InfoID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(relationshipinfo.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		parents, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "info_id" returned %v`, n.ID)
		}
		for i := range parents {
			parents[i].Edges.Info = n
		}
	}
	return nil
}
