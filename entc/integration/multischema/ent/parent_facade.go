// Copyright 2019-present Facebook Inc. All rights reserved.
// This source code is licensed under the Apache 2.0 license found
// in the LICENSE file in the root directory of this source tree.

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"

	"entgo.io/ent/entc/integration/multischema/ent/parent"
	"entgo.io/ent/entc/integration/multischema/ent/user"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
)

// Type aliases — public consumer-facing names continue resolving here
// while the underlying types live in the per-entity sub-package.
type (
	ParentClient     = parent.ParentClient
	ParentQuery      = parent.ParentQuery
	ParentMutation   = parent.ParentMutation
	ParentCreate     = parent.ParentCreate
	ParentCreateBulk = parent.ParentCreateBulk
	ParentUpdate     = parent.ParentUpdate
	ParentUpdateOne  = parent.ParentUpdateOne
	ParentDelete     = parent.ParentDelete
	ParentDeleteOne  = parent.ParentDeleteOne
	ParentGroupBy    = parent.ParentGroupBy
	ParentSelect     = parent.ParentSelect
)

// Constructor aliases — the sub-package's New<X> stays the source of truth.
var (
	NewParentClient = parent.NewParentClient
)

// WithParentChild eager-loads the "child" edge on a ParentQuery. The
// optional arguments configure the sibling sub-query before storage.
func WithParentChild(q *ParentQuery, opts ...func(*UserQuery)) *ParentQuery {
	sub := NewUserClient(q.Config).Query()
	for _, opt := range opts {
		opt(sub)
	}
	return q.StoreEager("child", func(ctx context.Context, parents []*Parent) error {
		return loadParentChild(ctx, sub, parents)
	})
}

// QueryParentChild returns a UserQuery for the "child" edge of a given Parent.
func QueryParentChild(c *ParentClient, _m *Parent) *UserQuery {
	query := NewUserClient(c.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(parent.Table, parent.FieldID, id),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, parent.ChildTable, parent.ChildColumn),
		)
		schemaConfig := _m.Config.SchemaConfig()
		step.To.Schema = schemaConfig.User
		step.Edge.Schema = schemaConfig.Parent
		fromV = sqlgraph.Neighbors(_m.Drv.Dialect(), step)

		return fromV, nil
	}
	return query
}

// loadParentChild performs the eager-load for the "child" edge. Body mirrors
// the pre-PR6 *ParentQuery.loadChild method, hoisted to root
// so it can reference cross-package types directly.
func loadParentChild(ctx context.Context, query *UserQuery, nodes []*Parent) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Parent)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		parents, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range parents {
			parents[i].Edges.Child = n
		}
	}
	return nil
}

// WithParentParent eager-loads the "parent" edge on a ParentQuery. The
// optional arguments configure the sibling sub-query before storage.
func WithParentParent(q *ParentQuery, opts ...func(*UserQuery)) *ParentQuery {
	sub := NewUserClient(q.Config).Query()
	for _, opt := range opts {
		opt(sub)
	}
	return q.StoreEager("parent", func(ctx context.Context, parents []*Parent) error {
		return loadParentParent(ctx, sub, parents)
	})
}

// QueryParentParent returns a UserQuery for the "parent" edge of a given Parent.
func QueryParentParent(c *ParentClient, _m *Parent) *UserQuery {
	query := NewUserClient(c.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(parent.Table, parent.FieldID, id),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, parent.ParentTable, parent.ParentColumn),
		)
		schemaConfig := _m.Config.SchemaConfig()
		step.To.Schema = schemaConfig.User
		step.Edge.Schema = schemaConfig.Parent
		fromV = sqlgraph.Neighbors(_m.Drv.Dialect(), step)

		return fromV, nil
	}
	return query
}

// loadParentParent performs the eager-load for the "parent" edge. Body mirrors
// the pre-PR6 *ParentQuery.loadParent method, hoisted to root
// so it can reference cross-package types directly.
func loadParentParent(ctx context.Context, query *UserQuery, nodes []*Parent) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Parent)
	for i := range nodes {
		fk := nodes[i].ParentID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		parents, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "parent_id" returned %v`, n.ID)
		}
		for i := range parents {
			parents[i].Edges.Parent = n
		}
	}
	return nil
}
