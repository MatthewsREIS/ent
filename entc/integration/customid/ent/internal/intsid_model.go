// Copyright 2019-present Facebook Inc. All rights reserved.
// This source code is licensed under the Apache 2.0 license found
// in the LICENSE file in the root directory of this source tree.

// Code generated by ent, DO NOT EDIT.

package internal

import (
	"fmt"
	"strings"

	// Guardrail: internal model package must remain import-cycle safe and must not import
	// generated root query/client packages (alias direction is root -> internal only).
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/entc/integration/customid/sid"
)

// IntSID is the model entity for the IntSID schema.

type IntSID struct {
	Config
	// ID of the ent.
	ID sid.ID `json:"id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the IntSIDQuery when eager-loading is set.
	Edges          IntSIDEdges `json:"edges"`
	int_sid_parent *sid.ID
	selectValues   sql.SelectValues
}

// IntSIDEdges holds the relations/edges for other nodes in the graph.
type IntSIDEdges struct {
	// Parent holds the value of the parent edge.
	Parent *IntSID `json:"parent,omitempty"`
	// Children holds the value of the children edge.
	Children []*IntSID `json:"children,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// SetLoadedTypes sets the loaded types state for eager-loading.
func (e *IntSIDEdges) SetLoadedTypes(lt [2]bool) {
	e.loadedTypes = lt
}

// IsLoaded returns whether the edge at the given index was loaded.
func (e IntSIDEdges) IsLoaded(idx int) bool {
	return e.loadedTypes[idx]
}

// ParentOrErr returns the Parent value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e IntSIDEdges) ParentOrErr() (*IntSID, error) {
	if e.Parent != nil {
		return e.Parent, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{Label: "IntSID"}
	}
	return nil, &NotLoadedError{Edge: "parent"}
}

// ChildrenOrErr returns the Children value or an error if the edge
// was not loaded in eager-loading.
func (e IntSIDEdges) ChildrenOrErr() ([]*IntSID, error) {
	if e.loadedTypes[1] {
		return e.Children, nil
	}
	return nil, &NotLoadedError{Edge: "children"}
}

// ScanValues returns the types for scanning values from sql.Rows.
func (*IntSID) ScanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case "id":
			values[i] = new(sid.ID)
		case "int_sid_parent": // int_sid_parent
			values[i] = &sql.NullScanner{S: new(sid.ID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// AssignValues assigns the values that were returned from sql.Rows (after scanning)
// to the IntSID fields.
func (_m *IntSID) AssignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case "id":
			if value, ok := values[i].(*sid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case "int_sid_parent":
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field int_sid_parent", values[i])
			} else if value.Valid {
				_m.int_sid_parent = new(sid.ID)
				*_m.int_sid_parent = *value.S.(*sid.ID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the IntSID.
// This includes values selected through modifiers, order, etc.
func (_m *IntSID) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// SetIntSidParent sets the int_sid_parent foreign key.
func (_m *IntSID) SetIntSidParent(v sid.ID) {
	_m.int_sid_parent = &v
}

// GetIntSidParent returns the int_sid_parent foreign key value.
func (_m *IntSID) GetIntSidParent() *sid.ID {
	return _m.int_sid_parent
}

// Unwrap unwraps the IntSID entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *IntSID) Unwrap() *IntSID {
	unwrapper, ok := _m.Config.Drv.(DriverUnwrapper)
	if !ok {
		panic("internal: IntSID is not a transactional entity")
	}
	_m.Config.Drv = unwrapper.UnwrapDriver()
	return _m
}

// String implements the fmt.Stringer.
func (_m *IntSID) String() string {
	var builder strings.Builder
	builder.WriteString("IntSID(")
	builder.WriteString(fmt.Sprintf("id=%v", _m.ID))
	builder.WriteByte(')')
	return builder.String()
}

// IntSIDs is a parsable slice of IntSID.
type IntSIDs []*IntSID
