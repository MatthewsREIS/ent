// Copyright 2019-present Facebook Inc. All rights reserved.
// This source code is licensed under the Apache 2.0 license found
// in the LICENSE file in the root directory of this source tree.

// Code generated by ent, DO NOT EDIT.

package edges

import (
	"context"
	"database/sql/driver"
	"fmt"

	"entgo.io/ent/examples/fs/ent/file"
	"entgo.io/ent/examples/fs/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
)

// LoadFileParent performs the eager-load for the "parent" edge. Body mirrors
// the pre-PR6 *FileQuery.loadParent method, hoisted to root
// so it can reference cross-package types directly.
func LoadFileParent(ctx context.Context, query *file.FileQuery, nodes []*file.File) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*file.File)
	for i := range nodes {
		fk := nodes[i].ParentID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(file.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		parents, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "parent_id" returned %v`, n.ID)
		}
		for i := range parents {
			parents[i].Edges.Parent = n
		}
	}
	return nil
}

// WithFileParent eager-loads the "parent" edge on a file.FileQuery. The
// optional arguments configure the sibling sub-query before storage.
func WithFileParent(q *file.FileQuery, opts ...func(*file.FileQuery)) *file.FileQuery {
	sub := file.NewFileClient(q.Config).Query()
	for _, opt := range opts {
		opt(sub)
	}
	return q.StoreEager("parent", func(ctx context.Context, parents []*file.File) error {
		return LoadFileParent(ctx, sub, parents)
	})
}

// QueryFileParent returns a file.FileQuery for the "parent" edge of a given file.File.
func QueryFileParent(c *file.FileClient, _m *file.File) *file.FileQuery {
	query := file.NewFileClient(c.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(file.Table, file.FieldID, id),
			sqlgraph.To(file.Table, file.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, file.ParentTable, file.ParentColumn),
		)
		fromV = sqlgraph.Neighbors(_m.Drv.Dialect(), step)

		return fromV, nil
	}
	return query
}

// QueryFileParentFromQuery returns a file.FileQuery that traverses the "parent" edge
// of every file.File matched by q (chained-query form). Mirrors the pre-PR6
// (*file.FileQuery).QueryParent method, hoisted to root so it
// can reference the cross-package file.FileQuery type.
func QueryFileParentFromQuery(q *file.FileQuery) *file.FileQuery {
	query := file.NewFileClient(q.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, err error) {
		if err := q.PrepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := q.SQLQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(file.Table, file.FieldID, selector),
			sqlgraph.To(file.Table, file.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, file.ParentTable, file.ParentColumn),
		)
		fromV = sqlgraph.SetNeighbors(q.Drv.Dialect(), step)
		return fromV, nil
	}
	return query
}

// LoadFileChildren performs the eager-load for the "children" edge. Body mirrors
// the pre-PR6 *FileQuery.loadChildren method, hoisted to root
// so it can reference cross-package types directly.
func LoadFileChildren(ctx context.Context, query *file.FileQuery, nodes []*file.File) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*file.File)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		nodes[i].Edges.Children = []*file.File{}
	}
	query.IncludeForeignKeys(true)
	if len(query.Ctx.Fields) > 0 {
		query.Ctx.AppendFieldOnce(file.FieldParentID)
	}
	query.Where(predicate.File(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(file.ChildrenColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ParentID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "parent_id" returned %v for node %v`, fk, n.ID)
		}
		node.Edges.Children = append(node.Edges.Children, n)
	}
	return nil
}

// WithFileChildren eager-loads the "children" edge on a file.FileQuery. The
// optional arguments configure the sibling sub-query before storage.
func WithFileChildren(q *file.FileQuery, opts ...func(*file.FileQuery)) *file.FileQuery {
	sub := file.NewFileClient(q.Config).Query()
	for _, opt := range opts {
		opt(sub)
	}
	return q.StoreEager("children", func(ctx context.Context, parents []*file.File) error {
		return LoadFileChildren(ctx, sub, parents)
	})
}

// QueryFileChildren returns a file.FileQuery for the "children" edge of a given file.File.
func QueryFileChildren(c *file.FileClient, _m *file.File) *file.FileQuery {
	query := file.NewFileClient(c.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(file.Table, file.FieldID, id),
			sqlgraph.To(file.Table, file.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, file.ChildrenTable, file.ChildrenColumn),
		)
		fromV = sqlgraph.Neighbors(_m.Drv.Dialect(), step)

		return fromV, nil
	}
	return query
}

// QueryFileChildrenFromQuery returns a file.FileQuery that traverses the "children" edge
// of every file.File matched by q (chained-query form). Mirrors the pre-PR6
// (*file.FileQuery).QueryChildren method, hoisted to root so it
// can reference the cross-package file.FileQuery type.
func QueryFileChildrenFromQuery(q *file.FileQuery) *file.FileQuery {
	query := file.NewFileClient(q.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, err error) {
		if err := q.PrepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := q.SQLQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(file.Table, file.FieldID, selector),
			sqlgraph.To(file.Table, file.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, file.ChildrenTable, file.ChildrenColumn),
		)
		fromV = sqlgraph.SetNeighbors(q.Drv.Dialect(), step)
		return fromV, nil
	}
	return query
}
