// Copyright 2019-present Facebook Inc. All rights reserved.
// This source code is licensed under the Apache 2.0 license found
// in the LICENSE file in the root directory of this source tree.

// Code generated by ent, DO NOT EDIT.

package edges

import (
	"context"
	"database/sql/driver"
	"fmt"

	"entgo.io/ent/examples/migration/ent/card"
	"entgo.io/ent/examples/migration/ent/payment"
	"entgo.io/ent/examples/migration/ent/predicate"
	"entgo.io/ent/examples/migration/ent/user"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
)

// LoadCardOwner performs the eager-load for the "owner" edge. Body mirrors
// the pre-PR6 *CardQuery.loadOwner method, hoisted to root
// so it can reference cross-package types directly.
func LoadCardOwner(ctx context.Context, query *user.UserQuery, nodes []*card.Card) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*card.Card)
	for i := range nodes {
		fk := nodes[i].OwnerID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		parents, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "owner_id" returned %v`, n.ID)
		}
		for i := range parents {
			parents[i].Edges.Owner = n
		}
	}
	return nil
}

// WithCardOwner eager-loads the "owner" edge on a card.CardQuery. The
// optional arguments configure the sibling sub-query before storage.
func WithCardOwner(q *card.CardQuery, opts ...func(*user.UserQuery)) *card.CardQuery {
	sub := user.NewUserClient(q.Config).Query()
	for _, opt := range opts {
		opt(sub)
	}
	return q.StoreEager("owner", func(ctx context.Context, parents []*card.Card) error {
		return LoadCardOwner(ctx, sub, parents)
	})
}

// QueryCardOwner returns a user.UserQuery for the "owner" edge of a given card.Card.
func QueryCardOwner(c *card.CardClient, _m *card.Card) *user.UserQuery {
	query := user.NewUserClient(c.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(card.Table, card.FieldID, id),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, card.OwnerTable, card.OwnerColumn),
		)
		fromV = sqlgraph.Neighbors(_m.Drv.Dialect(), step)

		return fromV, nil
	}
	return query
}

// QueryCardOwnerFromQuery returns a user.UserQuery that traverses the "owner" edge
// of every card.Card matched by q (chained-query form). Mirrors the pre-PR6
// (*card.CardQuery).QueryOwner method, hoisted to root so it
// can reference the cross-package user.UserQuery type.
func QueryCardOwnerFromQuery(q *card.CardQuery) *user.UserQuery {
	query := user.NewUserClient(q.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, err error) {
		if err := q.PrepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := q.SQLQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(card.Table, card.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, card.OwnerTable, card.OwnerColumn),
		)
		fromV = sqlgraph.SetNeighbors(q.Drv.Dialect(), step)
		return fromV, nil
	}
	return query
}

// LoadCardPayments performs the eager-load for the "payments" edge. Body mirrors
// the pre-PR6 *CardQuery.loadPayments method, hoisted to root
// so it can reference cross-package types directly.
func LoadCardPayments(ctx context.Context, query *payment.PaymentQuery, nodes []*card.Card) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*card.Card)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		nodes[i].Edges.Payments = []*payment.Payment{}
	}
	query.IncludeForeignKeys(true)
	if len(query.Ctx.Fields) > 0 {
		query.Ctx.AppendFieldOnce(payment.FieldCardID)
	}
	query.Where(predicate.Payment(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(card.PaymentsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CardID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "card_id" returned %v for node %v`, fk, n.ID)
		}
		node.Edges.Payments = append(node.Edges.Payments, n)
	}
	return nil
}

// WithCardPayments eager-loads the "payments" edge on a card.CardQuery. The
// optional arguments configure the sibling sub-query before storage.
func WithCardPayments(q *card.CardQuery, opts ...func(*payment.PaymentQuery)) *card.CardQuery {
	sub := payment.NewPaymentClient(q.Config).Query()
	for _, opt := range opts {
		opt(sub)
	}
	return q.StoreEager("payments", func(ctx context.Context, parents []*card.Card) error {
		return LoadCardPayments(ctx, sub, parents)
	})
}

// QueryCardPayments returns a payment.PaymentQuery for the "payments" edge of a given card.Card.
func QueryCardPayments(c *card.CardClient, _m *card.Card) *payment.PaymentQuery {
	query := payment.NewPaymentClient(c.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, _ error) {
		id := _m.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(card.Table, card.FieldID, id),
			sqlgraph.To(payment.Table, payment.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, card.PaymentsTable, card.PaymentsColumn),
		)
		fromV = sqlgraph.Neighbors(_m.Drv.Dialect(), step)

		return fromV, nil
	}
	return query
}

// QueryCardPaymentsFromQuery returns a payment.PaymentQuery that traverses the "payments" edge
// of every card.Card matched by q (chained-query form). Mirrors the pre-PR6
// (*card.CardQuery).QueryPayments method, hoisted to root so it
// can reference the cross-package payment.PaymentQuery type.
func QueryCardPaymentsFromQuery(q *card.CardQuery) *payment.PaymentQuery {
	query := payment.NewPaymentClient(q.Config).Query()
	query.Path = func(ctx context.Context) (fromV *sql.Selector, err error) {
		if err := q.PrepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := q.SQLQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(card.Table, card.FieldID, selector),
			sqlgraph.To(payment.Table, payment.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, card.PaymentsTable, card.PaymentsColumn),
		)
		fromV = sqlgraph.SetNeighbors(q.Drv.Dialect(), step)
		return fromV, nil
	}
	return query
}
